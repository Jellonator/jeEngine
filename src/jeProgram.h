/*#pragma once
#include "JE.h"
namespace JE{namespace GRAPHICS{
class Shader;
class Program
{
	public:
    std::vector<Shader*> shaders;
    GLuint program;
		Program();
		virtual ~Program();
		void addShader(Shader* shader);
		void link();
		void use();
	protected:
	private:
};
};};
*/
